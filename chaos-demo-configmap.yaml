apiVersion: v1
kind: ConfigMap
metadata:
  name: chaos-demo-html
  namespace: default
data:
  index.html: |
    <h1>Chaos Demo OK</h1>
    <p>NGINX is serving the Chaos Edge demo.</p>
  healthz: |
    ok
  chaos: |
    chaos-ok

